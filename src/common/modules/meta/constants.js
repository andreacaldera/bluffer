import { NAMESPACE } from '../constants';

export const SET_ACTIVE_PAGE = `${NAMESPACE}/meta/SET_ACTIVE_PAGE`;

export const DISPLAY_INFO = `${NAMESPACE}/meta/DISPLAY_INFO`;
export const DISPLAY_ERROR = `${NAMESPACE}/meta/DISPLAY_ERROR`;
